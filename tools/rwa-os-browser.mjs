import {chromium} from 'playwright';import {mkdir,writeFile} from 'node:fs/promises';
const base=process.env.RWA_OS_URL||'http://127.0.0.1:4173/rwa/rwa-os.html',proof=process.env.RWA_OS_PROOF_DIR||'proof/rwa-os-v14';await mkdir(proof,{recursive:true});const browser=await chromium.launch({headless:true});
async function pageAt(url){const p=await browser.newPage({viewport:{width:1600,height:1000}});const errors=[];p.on('pageerror',e=>errors.push(String(e)));p.on('console',m=>{if(m.type()==='error')errors.push(m.text())});await p.goto(url,{waitUntil:'networkidle'});return{p,errors}}
const live=await pageAt(base);if((await live.p.locator('#mode').innerText()).includes('FIXTURE'))throw Error('default page must not enable fixture');if(await live.p.locator('#kRevenue').innerText()!=='0')throw Error('default page fabricated revenue');if(await live.p.locator('#sellerCount').count())throw Error('unexpected legacy seller marker');await live.p.screenshot({path:`${proof}/01-default-live-gated.png`,fullPage:true});if(live.errors.length)throw Error(`default browser errors: ${live.errors.join(' | ')}`);await live.p.close();
const fx=await pageAt(base+'?fixture=1');await fx.p.waitForFunction(()=>window.RWAOS?.state?.distribution&&window.RWAOS?.state?.audit);if(!((await fx.p.locator('#mode').innerText()).includes('BROWSER TEST FIXTURE')))throw Error('fixture label missing');if(await fx.p.locator('#kSeries').innerText()!=='1')throw Error('series fixture missing');if(Number((await fx.p.locator('#rSettled').innerText()).replaceAll(',',''))!==1)throw Error('settled ledger count wrong');if(Number((await fx.p.locator('#rRefunded').innerText()).replaceAll(',',''))!==1)throw Error('refund ledger count wrong');if(Number((await fx.p.locator('#rEligible').innerText()).replaceAll(',',''))!==25000000)throw Error('refunded transaction contaminated eligible revenue');
for(const tab of ['series','pos','revenue','distribution','investor','audit']){await fx.p.locator(`[data-tab="${tab}"]`).click();if(!await fx.p.locator(`[data-panel="${tab}"]`).isVisible())throw Error(`panel ${tab} not visible`)}
await fx.p.locator('[data-tab="pos"]').click();if(!(await fx.p.locator('#barcodeRef').innerText()).startsWith('RWA-HOTELBALI001-'))throw Error('transaction barcode reference missing');if((await fx.p.locator('#barcode rect').count())<20)throw Error('barcode bars missing');await fx.p.screenshot({path:`${proof}/02-pos-barcode.png`,fullPage:true});
await fx.p.locator('[data-tab="distribution"]').click();const manifest=JSON.parse(await fx.p.locator('#distributionOut').innerText());if(manifest.status!=='AWAITING_AUTHORIZED_FUNDING_AND_PAYOUT')throw Error('distribution must stay funding-gated');if(manifest.entitlements.length!==2)throw Error('holder allocation count wrong');if(manifest.entitlements.reduce((n,x)=>n+x.amountMinor,0)!==manifest.allocatedMinor)throw Error('allocation conservation failed');if(!/^[a-f0-9]{64}$/.test(manifest.manifestHash))throw Error('manifest hash missing');await fx.p.screenshot({path:`${proof}/03-distribution.png`,fullPage:true});
await fx.p.locator('[data-tab="investor"]').click();await fx.p.locator('#findInvestor').click();if((await fx.p.locator('#investorOut').innerText()).includes('No entitlement'))throw Error('investor entitlement lookup failed');
await fx.p.locator('[data-tab="audit"]').click();const audit=JSON.parse(await fx.p.locator('#auditOut').innerText());if(audit.ledger.eligibleRevenueMinor!==25000000)throw Error('audit revenue mismatch');if(audit.ledger.refunded!==1)throw Error('audit refund mismatch');if(!/^[a-f0-9]{64}$/.test(audit.auditHash))throw Error('audit hash missing');await fx.p.screenshot({path:`${proof}/04-audit.png`,fullPage:true});if(fx.errors.length)throw Error(`fixture browser errors: ${fx.errors.join(' | ')}`);const result={ok:true,version:'14.0.0',defaultNoFakeRevenue:true,fixtureClearlyLabelled:true,series:true,posBarcode:true,settledOnlyRevenue:true,refundExcluded:true,distributionFundingGated:true,holderEntitlements:true,auditHash:true};await writeFile(`${proof}/browser-result.json`,JSON.stringify(result,null,2));console.log(JSON.stringify(result,null,2));await browser.close();
