(()=>{
'use strict';
if(window.RWARenkoV15MethodProfiles)return;
const STORE='rwa_renko_v15_method_profiles_v1',METHODS=['atr','traditional','percentage'];
const $=id=>document.getElementById(id),clamp=(v,a,b)=>Math.max(a,Math.min(b,v));
const parseNum=(v,f)=>{const n=Number(String(v??'').trim().replace(/,/g,'.'));return Number.isFinite(n)?n:f};
let engine=null,profiles=null,applying=false,lastActive='';
function defaults(){const c=engine?.settings||{},w=c.wicks!==false,confirm=Number(c.confirmBricks)===1?1:2;return{atr:{atrLength:clamp(Math.floor(Number(c.atrLength)||14),1,200),wicks:w,confirmBricks:confirm},traditional:{boxSize:Math.max(Number.EPSILON,Number(c.traditionalBox)||100),wicks:w,confirmBricks:confirm},percentage:{percentage:clamp((Number(c.percentage)||.01)*100,.0001,10),wicks:w,confirmBricks:confirm}}}
function normalize(p,b){p=p&&typeof p==='object'?p:{};const o={atr:{...b.atr,...(p.atr||{})},traditional:{...b.traditional,...(p.traditional||{})},percentage:{...b.percentage,...(p.percentage||{})}};o.atr.atrLength=clamp(Math.floor(Number(o.atr.atrLength)||14),1,200);o.traditional.boxSize=Math.max(Number.EPSILON,Number(o.traditional.boxSize)||100);o.percentage.percentage=clamp(Number(o.percentage.percentage)||1,.0001,10);for(const m of METHODS){o[m].wicks=o[m].wicks!==false;o[m].confirmBricks=Number(o[m].confirmBricks)===1?1:2}return o}
function load(){const b=defaults();try{return normalize(JSON.parse(localStorage.getItem(STORE)||'{}'),b)}catch{return b}}
function save(){try{localStorage.setItem(STORE,JSON.stringify(profiles))}catch{}}
function setFields(){const set=(id,v)=>{const e=$(id);if(e)e.value=String(v)},ck=(id,v)=>{const e=$(id);if(e)e.checked=!!v};set('v15AtrLength',profiles.atr.atrLength);ck('v15AtrWicks',profiles.atr.wicks);set('v15AtrConfirm',profiles.atr.confirmBricks);set('v15TraditionalBox',profiles.traditional.boxSize);ck('v15TraditionalWicks',profiles.traditional.wicks);set('v15TraditionalConfirm',profiles.traditional.confirmBricks);set('v15Percentage',profiles.percentage.percentage);ck('v15PercentageWicks',profiles.percentage.wicks);set('v15PercentageConfirm',profiles.percentage.confirmBricks)}
function syncActive(){const active=String(engine?.settings?.method||'atr');lastActive=active;document.querySelectorAll('[data-v15-profile]').forEach(p=>{const on=p.dataset.v15Profile===active;p.classList.toggle('active',on);if(!p.classList.contains('dirty')&&!p.classList.contains('applied')){const s=p.querySelector('.v15-state');if(s)s.textContent=on?'ACTIVE':'SAVED'}})}
function dirty(method){const p=document.querySelector(`[data-v15-profile="${method}"]`);if(!p||p.classList.contains('applying'))return;p.classList.add('dirty');p.classList.remove('applied');const s=p.querySelector('.v15-state');if(s)s.textContent='CHANGED'}
function bind(id,m,event='input'){const e=$(id);if(e)e.addEventListener(event,()=>dirty(m))}
function read(method){if(method==='atr'){profiles.atr.atrLength=clamp(Math.floor(parseNum($('v15AtrLength')?.value,profiles.atr.atrLength)),1,200);profiles.atr.wicks=!!$('v15AtrWicks')?.checked;profiles.atr.confirmBricks=Number($('v15AtrConfirm')?.value)===1?1:2}else if(method==='traditional'){profiles.traditional.boxSize=Math.max(Number.EPSILON,parseNum($('v15TraditionalBox')?.value,profiles.traditional.boxSize));profiles.traditional.wicks=!!$('v15TraditionalWicks')?.checked;profiles.traditional.confirmBricks=Number($('v15TraditionalConfirm')?.value)===1?1:2}else{profiles.percentage.percentage=clamp(parseNum($('v15Percentage')?.value,profiles.percentage.percentage),.0001,10);profiles.percentage.wicks=!!$('v15PercentageWicks')?.checked;profiles.percentage.confirmBricks=Number($('v15PercentageConfirm')?.value)===1?1:2}save()}
function patch(method){const p=profiles[method];if(method==='atr')return{atrLength:p.atrLength,wicks:p.wicks,confirmBricks:p.confirmBricks};if(method==='traditional')return{traditionalBox:p.boxSize,wicks:p.wicks,confirmBricks:p.confirmBricks};return{percentage:p.percentage/100,wicks:p.wicks,confirmBricks:p.confirmBricks}}
function roundSig1(v){v=Number(v);if(!(v>0))return v;const mag=10**Math.floor(Math.log10(v));return Math.round(v/mag)*mag}
function roundTick(v,t){if(!(v>0))return NaN;if(!(t>0))return v;const s=String(t),d=s.includes('e-')?Math.min(12,Number(s.split('e-')[1])||0):(s.includes('.')?Math.min(12,s.length-s.indexOf('.')-1):0);return Number((Math.max(1,Math.round(v/t))*t).toFixed(d))}
function expected(method){const st=engine?.state||{},tick=Number(st.tickSize)||Number.EPSILON;if(method==='traditional')return roundTick(profiles.traditional.boxSize,tick);if(method==='percentage')return roundTick(roundSig1(Math.abs(Number(st.ltpSnapshot))*profiles.percentage.percentage/100),tick);return roundTick(Number(st.atrValue),tick)}
function verify(method){const c=engine?.settings||{},p=profiles[method],box=Number(engine?.state?.box),exp=expected(method),tick=Number(engine?.state?.tickSize)||0;if(c.method!==method||c.wicks!==p.wicks||Number(c.confirmBricks)!==Number(p.confirmBricks))return false;if(method==='atr'&&Number(c.atrLength)!==p.atrLength)return false;if(method==='traditional'&&Math.abs(Number(c.traditionalBox)-p.boxSize)>Math.max(1e-12,p.boxSize*1e-10))return false;if(method==='percentage'&&Math.abs(Number(c.percentage)-p.percentage/100)>1e-12)return false;return Number.isFinite(box)&&Number.isFinite(exp)&&Math.abs(box-exp)<=Math.max(tick*.51,Math.abs(exp)*1e-9,1e-10)&&engine.uniformAudit().ok}
async function apply(method,readDraft=true){if(!METHODS.includes(method)||!engine||applying)return false;if(readDraft)read(method);const panel=document.querySelector(`[data-v15-profile="${method}"]`),button=panel?.querySelector('.v15-apply'),state=panel?.querySelector('.v15-state');applying=true;panel?.classList.add('applying');panel?.classList.remove('dirty','applied');document.querySelectorAll('.v15-apply').forEach(b=>b.disabled=true);if(state)state.textContent='APPLYING';try{const ok=await engine.setMethod(method,patch(method));if(!ok||!verify(method))throw Error('Active tick box does not match setup');panel?.classList.add('applied');if(state)state.textContent='APPLIED';syncActive();setTimeout(()=>{panel?.classList.remove('applied');syncActive()},650);return true}catch(e){console.error('[V15 profile]',e);panel?.classList.add('dirty');if(state)state.textContent='RETRY';return false}finally{applying=false;panel?.classList.remove('applying');document.querySelectorAll('.v15-apply').forEach(b=>b.disabled=false)}}
function wire(){document.querySelectorAll('[data-v15-apply]').forEach(b=>b.addEventListener('click',()=>void apply(b.dataset.v15Apply,true)));for(const [id,m,e] of [['v15AtrLength','atr','input'],['v15AtrWicks','atr','change'],['v15AtrConfirm','atr','change'],['v15TraditionalBox','traditional','input'],['v15TraditionalWicks','traditional','change'],['v15TraditionalConfirm','traditional','change'],['v15Percentage','percentage','input'],['v15PercentageWicks','percentage','change'],['v15PercentageConfirm','percentage','change']])bind(id,m,e);for(const [id,m] of [['v15AtrLength','atr'],['v15TraditionalBox','traditional'],['v15Percentage','percentage']])$(id)?.addEventListener('keydown',e=>{if(e.key==='Enter')void apply(m,true)})}
async function boot(){for(let i=0;i<400&&!window.RWARenkoV15;i++)await new Promise(r=>setTimeout(r,25));engine=window.RWARenkoV15;if(!engine)return;profiles=load();setFields();wire();for(let i=0;i<800&&(!engine.state.symbol||!(Number(engine.state.tickSize)>0)||!engine.state.ticks.length||engine.state.building);i++)await new Promise(r=>setTimeout(r,25));const current=METHODS.includes(engine.settings?.method)?engine.settings.method:'atr';if(engine.state.ticks.length&&!engine.state.building)await apply(current,false);syncActive();setInterval(()=>{if(applying)return;const active=String(engine?.settings?.method||'atr');if(active!==lastActive)syncActive()},500);window.RWARenkoV15MethodProfiles={version:'1.0.0',profiles,apply,verify,expected,storageKey:STORE,inputContract:'draft-never-overwritten',setupContract:'three-independent-compact-profiles',timeframeControl:false}}
boot().catch(e=>console.error('[RENKO V15 profiles]',e));
})();
