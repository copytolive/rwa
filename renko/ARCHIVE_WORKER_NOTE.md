The browser lifetime backfill must process one selected market at a time. It is intentionally fail-closed: if any archive segment is unavailable or cannot be decoded, the UI reports PARTIAL coverage instead of claiming COMPLETE. Full-history completion means every Binance Vision raw individual-trade archive segment discovered for that selected market has been processed, then live continuation is appended from `@trade`.