/* Instant pair-switch shell.
 * The previous confirmed chart stays visible while the next symbol's 1s source
 * is fetched. Historical viewport/depth is preserved across pair switches so a
 * user who has scrolled away from live does not snap back to the newest brick.
 */
(()=>{
'use strict';
if(window.RWARenkoSwitchFast?.version==='1.2.0')return;
const T=window.RWARenkoTV;if(!T?.loadSymbol)return;
const original=T.loadSymbol.bind(T);
let seq=0,active=null;
const stats={switches:0,completed:0,failed:0,aborted:0,firstFrameMaxMs:0,lastFirstFrameMs:0,lastLoadMs:0,maxLoadMs:0,historyCaptures:0,historyRestores:0,historyRestoreFailures:0,historyInherited:0,historySavedTarget:0};
const historyViews=new Map();
const split=s=>{s=String(s||'').toUpperCase().replace(/[^A-Z0-9]/g,'');for(const q of ['USDT','USDC','FDUSD','BTC','ETH','BNB'])if(s.endsWith(q)&&s.length>q.length)return[s.slice(0,-q.length),q];return[s,'']};
const norm=s=>{s=String(s||'').toUpperCase().replace(/[^A-Z0-9]/g,'');if(!s)return'BTCUSDT';if(!/(USDT|USDC|FDUSD|BTC|ETH|BNB)$/.test(s)&&/^[A-Z0-9]{2,10}$/.test(s))return s+'USDT';return s};
function el(id){return document.getElementById(id)}
function viewport(){return window.RWARenkoStableChart?.viewport||null}
function snapshot(){try{return viewport()?.snapshot?.()||null}catch{return null}}
function remember(symbol){const s=snapshot();if(symbol&&s&&!s.following){historyViews.set(symbol,{...s,symbol});stats.historyCaptures++;document.documentElement.dataset.renkoHistoryCaptured=symbol}return s}
function targetHistory(from,to,current){const saved=historyViews.get(to);if(saved){stats.historySavedTarget++;return{...saved,source:'saved-target'}}if(current&&!current.following&&from&&from!==to){stats.historyInherited++;return{...current,symbol:to,source:'inherited'}}return null}
async function restoreHistory(symbol,snap){if(!snap||snap.following)return false;try{T.state.following=false;const wanted=Math.max(1,Math.min(5,Number(snap.historyPages)||1));while(Number(T.state.historyPages||1)<wanted){const ok=await T.loadOlderPage();if(!ok)break;T.state.following=false}const ok=viewport()?.restore?.(snap)===true;if(ok){const now=snapshot();if(now&&!now.following)historyViews.set(symbol,{...now,symbol});stats.historyRestores++;document.documentElement.dataset.renkoHistoryRestoreSource=String(snap.source||'carry');return true}stats.historyRestoreFailures++;return false}catch(e){stats.historyRestoreFailures++;console.warn('[RENKO history restore]',e);return false}}
function overlay(){let e=el('switchOverlay');if(e)return e;const wrap=el('chartWrap');if(!wrap)return null;e=document.createElement('div');e.id='switchOverlay';e.className='switch-overlay';e.hidden=true;e.innerHTML='<span class="switch-dot"></span><b></b><small>Previous chart stays visible until the new 1s data is ready.</small>';wrap.appendChild(e);return e}
function showTarget(symbol,history){const [base,quote]=split(symbol),pair=el('pairName'),icon=el('pairIcon'),feed=el('feedPill'),o=overlay(),wrap=el('chartWrap');if(pair)pair.textContent=`${base} / ${quote}`;if(icon)icon.textContent=base.slice(0,2);if(feed){feed.classList.remove('live');const b=feed.querySelector('b');if(b)b.textContent='SWITCHING'}if(o){o.hidden=false;const b=o.querySelector('b');if(b)b.textContent=`Switching to ${base} / ${quote} · 1s`;const s=o.querySelector('small');if(s)s.textContent=history?'Historical viewport will be restored when the new 1s data is ready.':'Previous chart stays visible until the new 1s source is ready.'}wrap?.classList.add('switching');document.documentElement.dataset.pairSwitching='true';document.documentElement.dataset.pairSwitchTarget=symbol;document.documentElement.dataset.renkoHistoryCarry=history?'true':'false'}
function finish(symbol,ok,id,start){if(id!==seq){stats.aborted++;return}const elapsed=performance.now()-start,wrap=el('chartWrap'),o=overlay();wrap?.classList.remove('switching');if(o)o.hidden=true;document.documentElement.dataset.pairSwitching='false';document.documentElement.dataset.pairSwitchCompleted=ok?'true':'false';document.documentElement.dataset.pairSwitchLoadMs=elapsed.toFixed(3);stats.lastLoadMs=elapsed;stats.maxLoadMs=Math.max(stats.maxLoadMs,elapsed);if(ok)stats.completed++;else stats.failed++;active=null;window.dispatchEvent(new CustomEvent('renko:symbol-switch-end',{detail:{symbol,ok,elapsedMs:elapsed,id}}))}
async function load(symbol,opts={}){symbol=norm(symbol);if(!symbol)return false;if(active?.symbol===symbol)return active.promise;if(T.state?.symbol===symbol&&T.state?.status==='live'&&document.documentElement.dataset.pairSwitching!=='true')return true;const id=++seq,start=performance.now(),from=T.state?.symbol||'',current=remember(from),history=targetHistory(from,symbol,current);stats.switches++;showTarget(symbol,history);if(from&&from!==symbol&&T.settings){delete T.settings._exactBox;delete T.state.atrMatrixPrepared;delete T.state.atrHistorySatisfied;delete T.state.atrRaw;delete T.state.atrAppliedLength}window.dispatchEvent(new CustomEvent('renko:symbol-switch-start',{detail:{from,to:symbol,id,historyCarry:!!history}}));const first=performance.now()-start;stats.lastFirstFrameMs=first;stats.firstFrameMaxMs=Math.max(stats.firstFrameMaxMs,first);document.documentElement.dataset.pairSwitchFirstFrameMs=first.toFixed(3);document.documentElement.dataset.pairSwitchStartedAt=start.toFixed(3);const p=(async()=>{try{let ok=await original(symbol,{...opts,fit:history?false:opts.fit!==false});if(ok&&history)await restoreHistory(symbol,history);finish(symbol,!!ok,id,start);return !!ok}catch(e){finish(symbol,false,id,start);console.error('[RENKO fast switch]',e);return false}})();active={id,symbol,promise:p};return p}
function symbolFromRow(row){if(!row)return'';const direct=row.dataset?.symbol;if(direct)return norm(direct);const text=row.querySelector('.pair-main b')?.textContent||'';return norm(text.replace(/\s*\/\s*/g,''))}
document.addEventListener('click',e=>{const row=e.target?.closest?.('.pair-row');if(row){const s=symbolFromRow(row);if(s){e.preventDefault();e.stopPropagation();e.stopImmediatePropagation();document.querySelector('.markets')?.classList.remove('open');void load(s,{fit:true});return}}const quick=e.target?.closest?.('[data-quick]');if(quick){const s=norm(quick.dataset.quick);if(s){e.preventDefault();e.stopPropagation();e.stopImmediatePropagation();void load(s,{fit:true})}}},true);
T.loadSymbol=load;
window.RWARenkoSwitchFast={version:'1.2.0',load,stats,historyViews,get active(){return active},getHistorySnapshot:symbol=>historyViews.get(norm(symbol))||null};
})();
