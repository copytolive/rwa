RENKO FINAL ZERO-SAFE CLOSURE TRIGGER — 2026-08-30

Runtime-neutral path trigger only.

Purpose:
- force every current RENKO workflow with a renko/** path filter to execute against one exact main SHA after synchronizing stale workflow assertions with the current runtime and validating the current XAUT pack-v2 checksum contract;
- prove the zero-safe ATR runtime, Traditional controls, fixed-1s behavior, 50-pair history/zero-ms gates, XAUT path, public Pages deployment, and browser acceptance together;
- change no production JavaScript, CSS, data source, Renko math, UI behavior, or runtime semantics.

Acceptance remains strict. Positive raw Wilder ATR equals box exactly. A mathematically zero Wilder ATR falls back explicitly to the exchange minimum tick. Traditional remains fixed absolute 1x continuation / 2x reversal. The production source remains permanently fixed at 1 second. XAUT remains XAUT-USDT / OKX Spot / native 1s / >=1,000,000 rows / upsampled=false / synthetic1s=false, with the declared uncompressed CSV SHA256 verified against the exact pack bytes.
