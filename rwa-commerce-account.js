(()=>{
'use strict';
if(window.RWACommerceAccount)return;
const API=()=>window.RWACommerceAPI,$=id=>document.getElementById(id),esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));
const state={open:false,tab:'orders',busy:false,error:'',orders:[],stores:[],products:[],sellerOrders:[],selectedStore:''};
const money=(cents,currency='IDR')=>{const c=String(currency||'IDR').toUpperCase(),n=Number(cents||0)/100;try{return new Intl.NumberFormat('id-ID',{style:'currency',currency:c,maximumFractionDigits:c==='IDR'?0:2}).format(n)}catch{return`${c} ${n.toLocaleString()}`}};
const date=v=>{try{return new Intl.DateTimeFormat('id-ID',{dateStyle:'medium',timeStyle:'short'}).format(new Date(Number(v)||v))}catch{return'—'}};
const status=s=>`<span class="rwa-ca-status ${esc(String(s||'').toLowerCase())}">${esc(s||'—')}</span>`;
function style(){if($('rwaCommerceAccountStyle'))return;const s=document.createElement('style');s.id='rwaCommerceAccountStyle';s.textContent=`
.rwa-ca-layer{position:fixed;inset:0;z-index:10080;background:#05060af2;color:#eef2ff;display:none;overflow:auto;font:14px Inter,system-ui,sans-serif}.rwa-ca-layer.open{display:block}.rwa-ca-shell{width:min(1100px,calc(100% - 28px));margin:22px auto 60px;background:#0b0e15;border:1px solid #252c3b;border-radius:20px;box-shadow:0 30px 90px #0008;overflow:hidden}.rwa-ca-head{display:flex;justify-content:space-between;gap:16px;align-items:center;padding:18px 20px;border-bottom:1px solid #202635;position:sticky;top:0;background:#0b0e15f7;backdrop-filter:blur(14px);z-index:3}.rwa-ca-head small,.rwa-ca-muted{color:#8f9ab1}.rwa-ca-head h2{margin:3px 0 0}.rwa-ca-head button,.rwa-ca-btn,.rwa-ca-tabs button,.rwa-ca-form button{border:1px solid #30394d;background:#121724;color:#eef2ff;border-radius:10px;padding:9px 12px;cursor:pointer}.rwa-ca-btn.primary,.rwa-ca-form button.primary{background:#3457ff;border-color:#4c68ff}.rwa-ca-btn.danger{border-color:#66313b;color:#ff9ca7}.rwa-ca-head .close{font-size:22px;line-height:1}.rwa-ca-body{padding:20px}.rwa-ca-banner{padding:12px 14px;border:1px solid #27334a;border-radius:12px;background:#101624;margin-bottom:14px}.rwa-ca-banner.error{border-color:#67313c;color:#ff9ca7}.rwa-ca-tabs{display:flex;gap:8px;margin-bottom:16px}.rwa-ca-tabs button.active{background:#eef2ff;color:#0b0e15}.rwa-ca-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}.rwa-ca-card{border:1px solid #252c3b;background:#0f131d;border-radius:14px;padding:14px;min-width:0}.rwa-ca-card h3,.rwa-ca-card h4{margin:4px 0 8px}.rwa-ca-kv{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:8px;margin:10px 0}.rwa-ca-kv div{background:#0a0d13;border-radius:9px;padding:9px;min-width:0}.rwa-ca-kv small{display:block;color:#7f8aa1}.rwa-ca-kv b{display:block;overflow:hidden;text-overflow:ellipsis}.rwa-ca-actions{display:flex;gap:7px;flex-wrap:wrap;margin-top:10px}.rwa-ca-status{font-size:11px;font-weight:800;border-radius:99px;padding:4px 8px;background:#252c3b}.rwa-ca-status.paid,.rwa-ca-status.completed,.rwa-ca-status.ready_for_pickup{background:#123b30;color:#78efbd}.rwa-ca-status.awaiting_payment{background:#443515;color:#ffd978}.rwa-ca-status.cancelled,.rwa-ca-status.refunded{background:#4b2530;color:#ffb2bc}.rwa-ca-storebar{display:flex;gap:8px;overflow:auto;margin:12px 0}.rwa-ca-storebar button{white-space:nowrap}.rwa-ca-storebar button.active{border-color:#5d75ff;background:#182244}.rwa-ca-form{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}.rwa-ca-form label{display:grid;gap:5px}.rwa-ca-form input,.rwa-ca-form textarea,.rwa-ca-form select,.rwa-ca-stock{width:100%;box-sizing:border-box;background:#080b10;color:#eef2ff;border:1px solid #2b3344;border-radius:9px;padding:10px}.rwa-ca-form .wide{grid-column:1/-1}.rwa-ca-checks{display:flex;gap:15px;align-items:center}.rwa-ca-checks label{display:flex;gap:6px;align-items:center}.rwa-ca-table{display:grid;gap:8px}.rwa-ca-row{display:grid;grid-template-columns:1.4fr .8fr .7fr auto;gap:8px;align-items:center;padding:10px;border:1px solid #242b39;border-radius:10px}.rwa-ca-empty{padding:28px 16px;text-align:center;color:#929db4;border:1px dashed #2c3548;border-radius:12px}.rwa-ca-section{margin-top:18px}.rwa-ca-section-head{display:flex;justify-content:space-between;gap:12px;align-items:center;margin-bottom:9px}.rwa-ca-verify{display:flex;gap:6px;flex-wrap:wrap}.rwa-ca-verify span{font-size:10px;font-weight:800;border:1px solid #1e694f;color:#8af0c4;background:#102b22;padding:4px 7px;border-radius:99px}
@media(max-width:760px){.rwa-ca-shell{width:100%;margin:0;min-height:100%;border-radius:0}.rwa-ca-body{padding:14px}.rwa-ca-grid{grid-template-columns:1fr}.rwa-ca-form{grid-template-columns:1fr}.rwa-ca-form .wide{grid-column:auto}.rwa-ca-kv{grid-template-columns:1fr 1fr}.rwa-ca-row{grid-template-columns:1fr}.rwa-ca-head{padding:14px}.rwa-ca-actions .rwa-ca-btn{flex:1 1 auto}.rwa-ca-tabs{overflow:auto}.rwa-ca-tabs button{white-space:nowrap}}
`;document.head.appendChild(s)}
function shell(){style();if($('rwaCommerceAccountLayer'))return;const x=document.createElement('section');x.id='rwaCommerceAccountLayer';x.className='rwa-ca-layer';x.setAttribute('aria-label','RWA commerce account');x.innerHTML=`<div class="rwa-ca-shell"><header class="rwa-ca-head"><div><small>RWA COMMERCE ACCOUNT</small><h2>Orders & Seller Center</h2></div><button type="button" class="close" id="rwaCommerceAccountClose" aria-label="Close">×</button></header><div class="rwa-ca-body"><div class="rwa-ca-tabs"><button type="button" data-ca-tab="orders" class="active">My Orders</button><button type="button" data-ca-tab="seller">Seller Center</button></div><div id="rwaCommerceAccountBody"></div></div></div>`;document.body.appendChild(x)}
function inject(){shell();const a=document.querySelector('.rwa-shop-head-actions');if(a&&!a.querySelector('[data-commerce-account]')){const b=document.createElement('button');b.type='button';b.dataset.commerceAccount='1';b.textContent='Orders / Seller';a.insertBefore(b,a.querySelector('#rwaShopClose')||null)}const cmd=$('rwaCommandResults');if(cmd&&window.RWAStorefront?.open&&!document.querySelector('[data-commerce-account-top]')){} }
function orderCard(o){const s=String(o.status||''),canCancel=s==='AWAITING_PAYMENT',canPay=canCancel,canRefund=['PAID','FULFILLING','READY_FOR_PICKUP','SHIPPED','COMPLETED'].includes(s);return`<article class="rwa-ca-card"><div style="display:flex;justify-content:space-between;gap:10px"><div><small class="rwa-ca-muted">ORDER</small><h4>${esc(o.id)}</h4></div>${status(s)}</div><div class="rwa-ca-kv"><div><small>Total</small><b>${money(o.total_cents,o.currency)}</b></div><div><small>Fulfillment</small><b>${esc(o.fulfillment||'—')}</b></div><div><small>Created</small><b>${esc(date(o.created_at))}</b></div></div><div class="rwa-ca-actions">${canPay?`<button class="rwa-ca-btn primary" data-ca-pay="${esc(o.id)}">Pay now</button>`:''}${canCancel?`<button class="rwa-ca-btn danger" data-ca-cancel="${esc(o.id)}">Cancel</button>`:''}${canRefund?`<button class="rwa-ca-btn" data-ca-refund="${esc(o.id)}">Request refund</button>`:''}<button class="rwa-ca-btn" data-ca-payment-status="${esc(o.id)}">Payment status</button></div></article>`}
function sellerOrderCard(o){const s=String(o.status||''),next=s==='PAID'?['FULFILLING']:s==='FULFILLING'?(String(o.fulfillment)==='pickup'?['READY_FOR_PICKUP']:['SHIPPED']):['READY_FOR_PICKUP','SHIPPED'].includes(s)?['COMPLETED']:[];return`<article class="rwa-ca-card"><div style="display:flex;justify-content:space-between;gap:10px"><div><small class="rwa-ca-muted">SELLER ORDER</small><h4>${esc(o.id)}</h4></div>${status(s)}</div><div class="rwa-ca-kv"><div><small>Total</small><b>${money(o.total_cents,o.currency)}</b></div><div><small>Fulfillment</small><b>${esc(o.fulfillment||'—')}</b></div><div><small>Updated</small><b>${esc(date(o.updated_at))}</b></div></div>${next.length?`<div class="rwa-ca-actions">${next.map(n=>`<button class="rwa-ca-btn primary" data-ca-seller-status="${esc(o.id)}" data-next-status="${n}">${n==='FULFILLING'?'Start fulfillment':n==='READY_FOR_PICKUP'?'Ready for pickup':n==='SHIPPED'?'Mark shipped':'Complete order'}</button>`).join('')}</div>`:''}</article>`}
function productRow(p){return`<div class="rwa-ca-row"><div><b>${esc(p.name)}</b><small class="rwa-ca-muted" style="display:block">${esc(p.sku)} · ${esc(p.id)}</small></div><div><small class="rwa-ca-muted">Price</small><b>${money(p.price_cents,p.currency)}</b></div><div><small class="rwa-ca-muted">Available</small><b>${Number(p.available||0)}</b></div><div style="display:flex;gap:6px;align-items:center"><input class="rwa-ca-stock" style="width:92px" id="stock-${esc(p.id)}" type="number" min="${Number(p.reserved||0)}" value="${Number(p.on_hand||0)}" aria-label="On hand stock"><button class="rwa-ca-btn" data-ca-stock-save="${esc(p.id)}">Save stock</button></div></div>`}
function locked(msg='Commerce backend is not deployed yet.'){return`<div class="rwa-ca-banner"><b>Account remains fail-closed</b><p class="rwa-ca-muted" style="margin:5px 0 0">${esc(msg)} No order, stock or payment state is fabricated.</p></div>`}
function render(){shell();document.querySelectorAll('[data-ca-tab]').forEach(b=>b.classList.toggle('active',b.dataset.caTab===state.tab));const b=$('rwaCommerceAccountBody');if(!b)return;if(state.busy){b.innerHTML='<div class="rwa-ca-empty">Loading authoritative commerce account…</div>';return}if(state.error){b.innerHTML=`<div class="rwa-ca-banner error"><b>${esc(state.error)}</b></div><button class="rwa-ca-btn" data-ca-refresh>Retry</button>`;return}if(state.tab==='orders'){b.innerHTML=`<div class="rwa-ca-banner"><b>Buyer account</b><p class="rwa-ca-muted" style="margin:5px 0 0">Order and payment status is loaded from the authoritative commerce backend. Session is wallet-signed and separate from trading authorization.</p></div>${state.orders.length?`<div class="rwa-ca-grid">${state.orders.map(orderCard).join('')}</div>`:'<div class="rwa-ca-empty">No orders for this wallet yet.</div>'}`;return}const selected=state.stores.find(x=>x.token===state.selectedStore);b.innerHTML=`<div class="rwa-ca-banner"><b>Verified seller workspace</b><p class="rwa-ca-muted" style="margin:5px 0 0">Only wallets explicitly assigned to a verified physical store can manage its catalog, stock and fulfillment.</p></div>${state.stores.length?`<div class="rwa-ca-storebar">${state.stores.map(s=>`<button class="rwa-ca-btn ${s.token===state.selectedStore?'active':''}" data-ca-store="${esc(s.token)}">${esc(s.name)} · ${esc(s.token)}</button>`).join('')}</div>`:'<div class="rwa-ca-empty">This wallet is not assigned as owner/manager of a verified physical store.</div>'}${selected?`<section class="rwa-ca-card"><div class="rwa-ca-section-head"><div><small class="rwa-ca-muted">PHYSICAL STORE</small><h3>${esc(selected.name)}</h3><div class="rwa-ca-muted">${esc(selected.full_address)}</div></div><div class="rwa-ca-verify"><span>STORE VERIFIED</span><span>ASSET VERIFIED</span></div></div><form id="rwaSellerProductForm" class="rwa-ca-form"><label>Product ID<input id="rwaSellerProductId" maxlength="128" placeholder="SKU-001"></label><label>SKU<input id="rwaSellerSku" maxlength="80" required></label><label>Name<input id="rwaSellerName" maxlength="120" required></label><label>Price (IDR)<input id="rwaSellerPrice" type="number" min="1" step="1" required></label><label>Stock on hand<input id="rwaSellerStock" type="number" min="0" step="1" value="0" required></label><label class="wide">Description<textarea id="rwaSellerDescription" rows="2" maxlength="500"></textarea></label><div class="rwa-ca-checks wide"><label><input id="rwaSellerPickup" type="checkbox" checked> Pickup</label><label><input id="rwaSellerShipping" type="checkbox" checked> Shipping</label></div><button type="submit" class="primary wide">Save authoritative product</button></form></section><section class="rwa-ca-section"><div class="rwa-ca-section-head"><h3>Catalog & stock</h3><span class="rwa-ca-muted">${state.products.length} item(s)</span></div>${state.products.length?`<div class="rwa-ca-table">${state.products.map(productRow).join('')}</div>`:'<div class="rwa-ca-empty">No products in this verified store.</div>'}</section>`:''}<section class="rwa-ca-section"><div class="rwa-ca-section-head"><h3>Fulfillment orders</h3><span class="rwa-ca-muted">${state.sellerOrders.length} order(s)</span></div>${state.sellerOrders.length?`<div class="rwa-ca-grid">${state.sellerOrders.map(sellerOrderCard).join('')}</div>`:'<div class="rwa-ca-empty">No seller orders yet.</div>'}</section>`}
async function load({login=true}={}){state.busy=true;state.error='';render();try{if(!API()){throw Error('Commerce API unavailable')}if(!await API().enabled()){state.busy=false;state.error='Commerce backend is not deployed';render();return}if(login)await API().ensureLogin();const [o,s,so]=await Promise.all([API().orders(),API().sellerStores(),API().sellerOrders()]);state.orders=o.data||[];state.stores=s.data||[];state.sellerOrders=so.data||[];if(!state.selectedStore||!state.stores.some(x=>x.token===state.selectedStore))state.selectedStore=state.stores[0]?.token||'';state.products=state.selectedStore?((await API().sellerProducts(state.selectedStore)).data||[]):[]}catch(e){state.error=e?.code==='commerce_backend_locked'?'Commerce backend is not deployed':String(e?.message||e)}finally{state.busy=false;render()}}
async function open(tab='orders'){state.open=true;state.tab=tab;shell();$('rwaCommerceAccountLayer').classList.add('open');document.documentElement.style.overflow='hidden';await load()}
function close(){state.open=false;$('rwaCommerceAccountLayer')?.classList.remove('open');document.documentElement.style.overflow=''}
async function action(fn){if(state.busy)return;state.busy=true;state.error='';render();try{await fn();await load({login:false})}catch(e){state.error=String(e?.message||e);state.busy=false;render()}}
async function submitProduct(e){e.preventDefault();const token=state.selectedStore,sku=$('rwaSellerSku').value.trim(),name=$('rwaSellerName').value.trim(),priceIdr=Number($('rwaSellerPrice').value),onHand=Number($('rwaSellerStock').value),rawId=$('rwaSellerProductId').value.trim(),id=(rawId||`${token}-${sku}`).replace(/\s+/g,'-').slice(0,128);if(!sku||!name||!Number.isSafeInteger(priceIdr)||priceIdr<=0||!Number.isSafeInteger(onHand)||onHand<0){state.error='Valid SKU, name, whole-IDR price and stock are required';render();return}await action(async()=>{await API().sellerUpsertProduct({id,storeToken:token,sku,name,description:$('rwaSellerDescription').value.trim(),priceCents:priceIdr*100,currency:'IDR',onHand,pickup:$('rwaSellerPickup').checked,shipping:$('rwaSellerShipping').checked})})}
document.addEventListener('click',e=>{const openBtn=e.target.closest?.('[data-commerce-account]');if(openBtn){e.preventDefault();open();return}if(e.target.closest?.('#rwaCommerceAccountClose')){close();return}const tab=e.target.closest?.('[data-ca-tab]');if(tab){state.tab=tab.dataset.caTab;render();return}if(e.target.closest?.('[data-ca-refresh]')){load();return}const st=e.target.closest?.('[data-ca-store]');if(st){state.selectedStore=st.dataset.caStore;action(async()=>{state.products=(await API().sellerProducts(state.selectedStore)).data||[]});return}const pay=e.target.closest?.('[data-ca-pay]');if(pay){action(async()=>{const p=(await API().createPayment(pay.dataset.caPay)).data;if(p?.redirect_url)location.assign(p.redirect_url)});return}const cancel=e.target.closest?.('[data-ca-cancel]');if(cancel){action(()=>API().cancelOrder(cancel.dataset.caCancel));return}const ps=e.target.closest?.('[data-ca-payment-status]');if(ps){action(async()=>{const x=(await API().paymentStatus(ps.dataset.caPaymentStatus)).data;alert(`Payment status: ${x?.transaction_status||x?.status||'unknown'}`)});return}const refund=e.target.closest?.('[data-ca-refund]');if(refund){const reason=prompt('Refund reason (minimum 5 characters):','Customer requested refund');if(reason!==null)action(()=>API().requestRefund(refund.dataset.caRefund,reason));return}const stock=e.target.closest?.('[data-ca-stock-save]');if(stock){const v=Number($(`stock-${stock.dataset.caStockSave}`)?.value);if(Number.isSafeInteger(v)&&v>=0)action(()=>API().sellerSetInventory(stock.dataset.caStockSave,v));return}const ss=e.target.closest?.('[data-ca-seller-status]');if(ss){action(()=>API().sellerSetOrderStatus(ss.dataset.caSellerStatus,ss.dataset.nextStatus));return}},true);
document.addEventListener('submit',e=>{if(e.target?.id==='rwaSellerProductForm')submitProduct(e)},true);
new MutationObserver(inject).observe(document.documentElement,{subtree:true,childList:true});
window.RWACommerceAccount={version:'1.0.0',runtime:'authoritative-buyer-seller-account-v1',open,close,reload:load,state:()=>({open:state.open,tab:state.tab,orders:state.orders.length,sellerStores:state.stores.length,sellerProducts:state.products.length,sellerOrders:state.sellerOrders.length})};
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',inject,{once:true});else inject();
})();
