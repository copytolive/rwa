{
  "name": "rwa-token-pre-tge",
  "private": true,
  "version": "0.3.0",
  "description": "PRE-TGE platform token plus non-transferable redeemable Product RWA MVP contract verification; no automatic deployment",
  "scripts": {
    "compile": "rm -rf build && mkdir -p build && solcjs --base-path . --include-path node_modules --abi --bin contracts/RWAToken.sol contracts/RWAVestingVault.sol contracts/ProductInventoryGate.sol contracts/ProductRWA1155.sol contracts/RedemptionManager.sol contracts/interfaces/*.sol -o build",
    "test": "node test/contracts.test.mjs && node test/product-rwa.test.mjs",
    "verify": "npm run compile && npm test"
  },
  "devDependencies": {
    "@openzeppelin/contracts": "5.1.0",
    "ethers": "6.13.4",
    "ganache": "7.9.2",
    "solc": "0.8.24"
  }
}
