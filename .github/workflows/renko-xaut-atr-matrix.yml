name: RENKO XAUT Fixed 1s ATR Matrix Gate — Legacy Manual Only
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  legacy-scope:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'XAUT is outside the active RENKO GOLD/XAUUSD scope; retained for manual historical reference only.'
