name: RENKO Total Available History 50 + GOLD — Legacy Manual Only
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  legacy-scope:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy 50-pair history gate is no longer an automatic main gate. GOLD/XAUUSD canonical history is authoritative.'
