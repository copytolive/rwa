name: AI5 Public Runner Probe
on:
  push:
    branches: [main]
    paths:
      - "ai5-public-runner-probe/**"
jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - run: echo AI5_PUBLIC_RUNNER_PASS
