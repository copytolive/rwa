name: GOLD Canonical v11 Auto Backtest (PAUSED BY V2 GATE A)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  gate-a-fail-closed:
    runs-on: ubuntu-latest
    steps:
      - name: V2 governance guard
        run: |
          echo 'strategy_engine=PAUSED'
          echo 'policy=GOLD24_WEEKLY_PROFIT_RR12_V2_20260828'
          echo 'gate_a=BLOCKED_DRIVE_APPROVED_CROSSCHECK_BYTES_RECEIPT_MISSING'
          echo 'Legacy v11 simulation is disabled until the canonical V2 Gate A passes.'
          echo 'No strategy simulation, counter increment, TOP200 change, or TOP100_COMPLIANT change is permitted.'
