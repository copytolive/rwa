name: RENKO 50 Pair Full History 10x Zero-ms Gate — Legacy Manual Only
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  legacy-scope:
    runs-on: ubuntu-latest
    steps:
      - run: echo '50-pair history scope is retired from automatic main; GOLD canonical full-history closure is authoritative.'
