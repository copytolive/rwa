name: RENKO Fixed 1s Canonical Compatibility Gate — Legacy Manual Only
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  legacy-scope:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy compatibility gate retained manually. GOLD fixed-1s canonical compatibility is enforced by the GOLD current-main closure.'
