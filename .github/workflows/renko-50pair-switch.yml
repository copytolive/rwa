name: RENKO 50 Pair Zero-ms Switch Gate — Legacy Manual Only
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  legacy-scope:
    runs-on: ubuntu-latest
    steps:
      - run: echo '50-pair switch scope is retired from automatic main; active production scope is GOLD/XAUUSD only.'
