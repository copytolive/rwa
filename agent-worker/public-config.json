{
  "schema": 2,
  "enabled": false,
  "candidate_base_url": "https://seablueprint.com/rwa-worker",
  "base_url": "",
  "mode": "delegated-agent",
  "master_private_key_required": false,
  "agent_private_key_scope": "trade-only delegated Hyperliquid agent",
  "note": "candidate_base_url is probe-only. Set enabled=true and base_url only after public /healthz + /readyz prove the TESTNET worker is actually live with kill_switch=false and mainnet_allowed=false."
}
