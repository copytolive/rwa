# Security model

RWA Network is non-custodial. Browser wallet providers sign wallet links and exchange orders. Private keys must never be committed to GitHub, embedded in client JavaScript, or stored in localStorage. Mainnet execution requires an explicit real-funds acknowledgement; testnet is the default. Verified trader metrics are derived from public venue data only after a signed wallet link. RWA drafts are never labeled verified unless included in the public registry.
