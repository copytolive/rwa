# Secret handling

Do not commit Oracle SSH private keys, Google service-account JSON, passwords, API tokens, or GitHub secret values to this public repository.

Use GitHub environment secrets and root-owned files on the persistent VM only.
